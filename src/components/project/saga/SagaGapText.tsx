import styles from './saga.module.css';

export default function SagaGapText() {
  return (
    <section className={styles.gapTextSection}>
      <p className={styles.gapText}>
        The gap between high emotional bond and low physical affection creates
        an unfulfilled need
        <br />: taking photos together.
      </p>
    </section>
  );
}
