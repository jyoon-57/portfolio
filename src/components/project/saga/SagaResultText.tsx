import styles from './saga.module.css';

export default function SagaResultText() {
  return (
    <section className={styles.resultTextSection}>
      <p className={styles.resultText}>
        By the end of the usage period, the resulting film will preserve the
        natural, candid moments of parents and children—sides of themselves they
        may have never even noticed.
      </p>
    </section>
  );
}
